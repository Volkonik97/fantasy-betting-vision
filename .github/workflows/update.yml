name: 📊 Update Supabase from Oracle's Elixir CSV

on:
  workflow_dispatch:
  schedule:
    - cron: '0 7 * * *' # Tous les jours à 7h du matin UTC

jobs:
  update-supabase:
    runs-on: ubuntu-latest

    env:
      SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
      SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
      GOOGLE_FILE_ID: ${{ secrets.GOOGLE_FILE_ID }}

    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v3

      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'

      - name: 📦 Install dependencies
        run: npm install node-fetch@2 papaparse @supabase/supabase-js --legacy-peer-deps

      - name: 🚀 Run update script
        run: node scripts/updateSupabase.js
